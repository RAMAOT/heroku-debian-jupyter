setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
build:
  docker:
    web: Dockerfile
  languages:
    - python
run:
  web: python3 -m notebook --no-browser --no-mathjax --ip=* --port=$PORT
